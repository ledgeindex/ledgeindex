# @ledgeindex/web

Next.js UI for LedgeIndex (sources, ingest, docs chat).

```bash
# From ledgeindex monorepo root:
npm run dev:web
```

Env: copy [`../../env.example`](../../env.example) and set `NEXT_PUBLIC_*` in `.env.local` as needed.
